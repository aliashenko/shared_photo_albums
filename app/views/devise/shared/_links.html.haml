.divider
.devise-shared-links
  - if controller_name != 'sessions'
    = link_to "Log in", new_session_path(resource_name), class: 'btn teal lighten-5 black-text'
  - if devise_mapping.registerable? && controller_name != 'registrations'
    = link_to "Sign up", new_registration_path(resource_name), class: 'btn teal lighten-5 black-text'

  - if devise_mapping.omniauthable?
    - resource_class.omniauth_providers.each do |provider|
      = link_to omniauth_authorize_path(resource_name, provider), class: 'google-button btn teal lighten-5 black-text' do
        %span Sign in with google
